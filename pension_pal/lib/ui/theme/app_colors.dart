import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color black = Color(0xFF000000);
  static const Color mustard = Color(0xFFFFDB58);
  static const Color yellow = Color(0xFFFFFF00);

  // Secondary colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Color(0xFF808080);

  // Functional colors
  static const Color error = Color(0xFFFF0000);
  static const Color success = Color(0xFF00FF00);

  // Background colors
  static const Color primaryBackground = black;
  static const Color secondaryBackground = mustard;

  // Text colors
  static const Color primaryText = white;
  static const Color secondaryText = grey;
  static const Color accentText = yellow;

  // Button colors
  static const Color primaryButtonBackground = mustard;
  static const Color primaryButtonText = black;
  static const Color secondaryButtonBackground = black;
  static const Color secondaryButtonText = mustard;

  // Input field colors
  static const Color inputBackground = white;
  static const Color inputText = black;
  static const Color inputBorder = grey;

  // App bar colors
  static const Color appBarBackground = black;
  static const Color appBarText = white;

  // Divider color
  static const Color divider = grey;

  // Icon colors
  static const Color primaryIcon = mustard;
  static const Color secondaryIcon = white;
}
